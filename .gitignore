# Generated Gen-5 sprite host pack (built by `deno task build:sprites`)
/assets/

# Deno (npm: deps are globally cached; node_modules only if nodeModulesDir is enabled)
node_modules/
vendor/

# Build output / logs / OS cruft
dist/
*.log
.DS_Store

# Git worktrees + subagent-driven-development progress ledger (local scratch)
.worktrees/
.superpowers/
